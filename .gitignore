# Build output — compiled libraries and headers installed here
sandbox/

# Downloaded source tarballs
downloads/

# Extracted and compiled library source trees
build/

# Build logs and skip-on-rerun touchfiles
logs/

# Visual Studio
.vs/

# macOS
.DS_Store

# ffmpeg configure log (written to repo root during configure)
config.log

# Meson cross-compilation file (generated at build time)
meson-cross.mingw.txt

cross_compilers/

# User's saved feature selections — generated by build.sh
build.cfg